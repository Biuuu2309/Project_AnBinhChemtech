# Lộ trình triển khai — Quotation Automation Prototype

**Phiên bản hợp nhất** từ `roadmap.md` + `roadmap-quotation-automation.docx`.  
Tham chiếu: `Describle.txt`, `solution-plan-vi.md`.  
Phạm vi: prototype E2E trong ~2–4 giờ. Deadline: **25/09/2026**.

---

## Quyết định kỹ thuật (chốt trước khi code)

| Quyết định | Lựa chọn | Lý do |
|---|---|---|
| Folder automation | `worker/` (giữ repo hiện tại) | Không đổi tên giữa chừng |
| Cách Mac mini nhận job | **Worker poll** Backend | Giống hệ thống riêng; mock dễ; backend không phụ thuộc process worker |
| Tạo file báo giá | `python-docx` deterministic | Testable, tái lập |
| Codex CLI | Stub interface `worker/codex/codex_runner.py` | Không dùng trong happy path prototype |
| DB | SQLite | Đủ cho prototype |
| Auth | JWT tối thiểu **hoặc** ghi “mock” trong README | Không over-engineer |

### Luồng kỹ thuật chốt

```text
React → FastAPI (validate) → SQLite (PENDING)
                                    ↑ poll
                              Python Worker (Mock Mac mini)
                                    ↓
                         TemplateService → DOCX → Validator
                                    ↓
                         COMPLETED / FAILED (+ download)
```

```text
AutomationService (interface)
  ├── MockWorkerPoll          ← Prototype (worker process)
  └── MacMiniAgent            ← Production (thay sau)
```

---

## Tổng quan phase

```text
Phase 0  Foundation + Plan          ✅ Done
Phase 1  Setup chạy được            ✅ Done
Phase 2  DB + Backend API + state   ✅ Done
Phase 3  Template + DOCX + Worker   ✅ Done
Phase 4  Frontend E2E               ✅ Done
Phase 5  Error / Retry / Logging    ✅ Done
Phase 6  README + Demo + Nộp        ✅ Done
```

| Phase | Mục tiêu | Ước lượng | Ưu tiên |
|-------|----------|-----------|---------|
| 0 | Plan + cấu trúc repo | — | ✅ Done |
| 1 | FE/BE/Worker chạy skeleton | 15–20’ | ✅ Done |
| 2 | Models, seed, API, job status | 40–50’ | ✅ Done |
| 3 | DOCX template + worker poll | 45–60’ | ✅ Done |
| 4 | Customer → Form → Status → Download | 40–50’ | ✅ Done |
| 5 | Retry, anti-dup, validation edge, logs | 20–25’ | ✅ Done |
| 6 | README, sample file, demo | 20–30’ | ✅ Done |

> **Must** = cần để demo đạt yêu cầu bài.  
> **Should** = làm nếu còn giờ; không kịp thì mô tả hướng xử lý trong README.

---

## Phase 0 — Foundation *(Done)*

- [x] Cấu trúc `frontend/`, `backend/`, `worker/`
- [x] Solution Plan + sơ đồ
- [x] `.env.example`, requirements skeleton
- [x] Roadmap hợp nhất (file này)

---

## Phase 1 — Setup chạy được *(Must)* ✅

**Mục tiêu:** 3 process chạy local trước khi viết nghiệp vụ.

1. [x] Backend: venv + `pip install -r requirements.txt` → `/health` OK, Swagger tại `/docs`
2. [x] Frontend: Tailwind (`@tailwindcss/vite`) + build/dev OK
3. [x] Worker: venv + deps + `python main.py` in “Worker ready…”
4. [x] Copy `.env.example` → `.env`

**Done when:** mở được Swagger + Vite + worker process (dù chưa có job thật).

---

## Phase 2 — Database + Backend API + Job state *(Must)* ✅

**Mục tiêu:** Backend là **source of truth** cho quotation và status.

### Models / seed
- [x] `Customer`, `Quotation`, `QuotationItem`
- [x] Status: `PENDING` → `PROCESSING` → `COMPLETED` | `FAILED`
- [x] Seed 2 khách hàng giả (`CUS-001`, `CUS-002`)

### API công khai
| Method | Path | Việc |
|--------|------|------|
| GET | `/api/customers` | Danh sách |
| GET | `/api/customers/{id}` | Chi tiết |
| POST | `/api/quotations` | Tạo job → `PENDING` |
| GET | `/api/quotations/{id}` | Status + metadata |
| GET | `/api/quotations/{id}/download` | Tải file (file thật ở Phase 3) |
| POST | `/api/quotations/{id}/retry` | FAILED → PENDING |

### API nội bộ (worker poll)
| Method | Path | Việc |
|--------|------|------|
| GET | `/api/internal/jobs/next` | Claim `PENDING` → `PROCESSING` (204 nếu hết) |
| POST | `/api/internal/jobs/{id}/complete` | → `COMPLETED` |
| POST | `/api/internal/jobs/{id}/fail` | → `FAILED` |

**Done when:** Swagger/TestClient tạo quotation `PENDING`, GET status đúng. ✅

---

## Phase 3 — Template DOCX + Worker *(Must)* ✅

**Mục tiêu:** Chứng minh automation tạo file từ mẫu (deterministic).

1. [x] `worker/templates/quotation_template.docx`
2. [x] `worker/jobs/template_service.py`
3. [x] `worker/jobs/validator.py`
4. [x] Worker loop: poll → generate → validate → complete/fail (`--once` hỗ trợ test)
5. [x] `worker/codex/codex_runner.py` stub (không gọi trong happy path)
6. [x] Output `worker/output/QT-….docx` + download API

**Done when:** POST quotation + worker → file DOCX tải được. ✅

---

## Phase 4 — Frontend E2E *(Must)* ✅

**Mục tiêu:** Demo đúng workflow nghiệp vụ (UI đơn giản).

1. [x] Customer List / Customer Detail + nút **Tạo báo giá**
2. [x] Quotation Form
3. [x] Submit → poll status mỗi 2s
4. [x] `COMPLETED` → **Tải báo giá**
5. [x] `fetch` + React Router

**Done when:** trên browser: xem KH → form → gửi → nhận file. ✅

---

## Phase 5 — Reliability *(Should)* ✅

1. [x] Retry: `FAILED` → `PENDING` (không tạo job mới)
2. [x] Anti-duplicate: cùng customer `PENDING`/`PROCESSING` → trả quotation hiện có (HTTP 200)
3. [x] Logging lifecycle (`quotation_created`, `deduplicated`, `automation_started`, `completed`, `failed`, `retry`)
4. [x] Auth: ghi rõ **chưa làm / mock** trong README (không JWT trong prototype)
5. [x] Test: `backend/tests/test_quotations.py` (422, anti-dup, fail, retry, download missing → 404)

**Done when:** retry + validation + anti-dup chạy; auth ghi trong README. ✅

---

## Phase 6 — README + Demo + Nộp *(Must)* ✅

| Hạng mục | Nội dung |
|----------|----------|
| Solution Plan | `doc/solution-plan-vi.md` (+ EN) |
| Source | Repo chạy được |
| README | Setup 3 process; Completed / Mocked / Unimplemented; Assumptions; AI; Production |
| Demo | Hướng dẫn local trong README |
| Sample | `doc/samples/QT-2026-SAMPLE.docx` |

**Done when:** người khác làm theo README chạy được E2E. ✅

---

## Lịch gợi ý 4 giờ

| Thời gian | Việc |
|-----------|------|
| 0:00–0:20 | Phase 1 — setup chạy được |
| 0:20–1:10 | Phase 2 — DB + API + status |
| 1:10–2:10 | Phase 3 — template + worker DOCX |
| 2:10–3:00 | Phase 4 — Frontend E2E |
| 3:00–3:25 | Phase 5 — retry / lỗi / log (nếu còn giờ) |
| 3:25–4:00 | Phase 6 — README + sample + demo |

---

## Nếu thiếu thời gian — cắt theo thứ tự

```text
1. Giữ Phase 2+3+download     → chứng minh tạo file
2. Phase 4 tối thiểu          → chứng minh UX workflow
3. Phase 6 README + sample    → nộp được
4. Phase 5 retry/anti-dup     → điểm cộng
5. JWT đầy đủ / PDF / Docker / Mac mini thật / Codex thật  → bỏ, chỉ mô tả
```

**Không làm trong prototype:** CRM đầy đủ, PostgreSQL, Redis queue, RBAC phức tạp.

---

## Ranh giới AI / Codex CLI

```text
Không bắt đầu bằng Codex.
Xây pipeline deterministic trước.
Codex chỉ sau interface rõ ràng (codex_runner.py).
Template mapping + validation + state machine = code thường.
```

---

## Checklist Definition of Done (toàn dự án)

- [x] Mở chi tiết khách hàng giả lập
- [x] Tạo báo giá qua form và gửi
- [x] Theo dõi được `PENDING` / `PROCESSING` / `COMPLETED` / `FAILED`
- [x] Worker (mock Mac mini) tạo DOCX từ template
- [x] Download file hoàn chỉnh
- [x] README nêu rõ phần mock (Mac mini / Codex)
- [x] Giải thích được: gửi data, nhận job (poll), chạy task, lưu kết quả, status

---

## Cấu trúc repo mục tiêu (khớp hiện tại)

```text
Project_AnBinhChemtech/
├── frontend/src/{pages,components,api,types,hooks}/
├── backend/app/{api,models,schemas,services,core}/
├── worker/{jobs,templates,output,codex}/
├── doc/{Describle,TechStack,solution-plan*,roadmap.md}
├── .env.example
└── README.md
```

---

## Bước tiếp theo

Prototype đã đủ phạm vi Must (Phase 0–6).  
Trước hạn **25/09/2026**: đẩy GitHub (nếu cần), gắn link/repo + `doc/samples/QT-2026-SAMPLE.docx` vào email nộp bài; optional quay video demo 3–5’.
