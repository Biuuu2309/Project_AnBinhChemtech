# Sample quotation output

`QT-2026-SAMPLE.docx` — file báo giá mẫu sinh từ template + deterministic fill (cùng pipeline worker).

Dùng để đính kèm bài nộp / minh họa output khi không chạy live demo.
